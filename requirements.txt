# Curiosity Engine — Dependencies

# LLM Providers (install the one you're using)
openai>=1.30.0
anthropic>=0.30.0

# Web Search
tavily-python>=0.7.0

# Multimodal Input
opencv-python>=4.8.0
sounddevice>=0.5.0

# Configuration
pyyaml>=6.0

# Embeddings (for full scoring)
numpy>=1.24.0

# Future dependencies (not needed for POC):
# sentence-transformers   # local embeddings
# chromadb                # vector database
# opencv-python           # webcam capture
# sounddevice             # audio capture
# openai-whisper          # speech-to-text
